{
  "name": "license-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for DocSync & DepGuard license key delivery",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
